// Стартовый экран Lingua-AI — приветствие + знакомство (клиентский HomeScreen).
import { HomeScreen } from "@/components/home/HomeScreen";

export default function Home() {
  return <HomeScreen />;
}
